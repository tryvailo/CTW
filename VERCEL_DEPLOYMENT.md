# 🚀 ДЕПЛОЙ НА VERCEL: РАБОТА С ДАННЫМИ

## ✅ ДА, ДАННЫЕ БУДУТ ЗАГРУЖАТЬСЯ ИЗ CSV ФАЙЛОВ!

**Короткий ответ:** Да, все будет работать на Vercel. Данные загружаются во время build time и встраиваются в статические страницы.

---

## 🔍 КАК ЭТО РАБОТАЕТ

### 1. Текущая реализация

**Файл:** `lib/data.ts`

```typescript
// Путь к данным
const DATA_DIR = path.join(process.cwd(), 'public', 'data');

// Загрузка CSV файла
function loadCSVFile(filePath: string): string {
  const fullPath = path.join(DATA_DIR, filePath);
  return fs.readFileSync(fullPath, 'utf-8'); // ✅ Работает на build time
}
```

### 2. Как это работает на Vercel

#### Build Time (на сервере Vercel):
1. ✅ Vercel клонирует ваш репозиторий
2. ✅ Запускает `npm run build`
3. ✅ Next.js читает CSV файлы из `public/data/` через `fs.readFileSync`
4. ✅ Генерирует статические HTML страницы (SSG)
5. ✅ Данные встраиваются в HTML страницы

#### Runtime (после деплоя):
- ✅ Все страницы уже статические HTML
- ✅ Данные уже встроены в страницы
- ✅ Нет необходимости читать CSV файлы в runtime
- ✅ Быстрая загрузка (CDN Vercel)

---

## 📁 СТРУКТУРА ДАННЫХ

### Где находятся файлы:
```
public/data/
├── procedures.csv      ✅ Будет доступен
├── nhs_waits.csv       ✅ Будет доступен
├── private_costs.csv   ✅ Будет доступен
├── clinics.csv         ✅ Будет доступен
└── cities.csv          ✅ Будет доступен
```

### Как они используются:

1. **Build Time (Vercel):**
   ```typescript
   // lib/data.ts читает файлы
   const content = fs.readFileSync('public/data/procedures.csv', 'utf-8');
   ```

2. **Static Generation:**
   ```typescript
   // app/comparison/[procedure]/[city]/page.tsx
   export async function generateStaticParams() {
     // Генерирует все 15 страниц сравнения
     return combinations.map(...)
   }
   ```

3. **Результат:**
   - 28 статических HTML страниц
   - Данные встроены в каждую страницу
   - Нет запросов к CSV файлам в runtime

---

## ✅ ПРОВЕРКА ПЕРЕД ДЕПЛОЕМ

### 1. Убедитесь что файлы на месте:

```bash
# Проверьте что все CSV файлы есть
ls -la public/data/*.csv

# Должны быть:
# - procedures.csv
# - nhs_waits.csv
# - private_costs.csv
# - clinics.csv
```

### 2. Проверьте сборку локально:

```bash
# Соберите проект
npm run build

# Проверьте что нет ошибок
# Должно быть: "Generating static pages (28/28)"
```

### 3. Проверьте что данные загружаются:

```bash
# Запустите production сервер
npm run start

# Откройте http://localhost:3000
# Проверьте что данные отображаются
```

---

## 🚀 ДЕПЛОЙ НА VERCEL

### Вариант 1: Через Vercel Dashboard (рекомендуется)

1. **Подключите GitHub репозиторий:**
   - Зайдите на https://vercel.com
   - Нажмите "New Project"
   - Подключите ваш GitHub репозиторий

2. **Настройки проекта:**
   - **Framework Preset:** Next.js (автоматически определится)
   - **Root Directory:** `./` (если проект в корне)
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)

3. **Environment Variables (если нужны):**
   - `NEXT_PUBLIC_BASE_URL` = `https://your-domain.vercel.app`

4. **Deploy:**
   - Нажмите "Deploy"
   - Vercel автоматически:
     - Установит зависимости
     - Запустит `npm run build`
     - Прочитает CSV файлы из `public/data/`
     - Сгенерирует 28 статических страниц
     - Задеплоит на CDN

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Деплой
vercel

# Production деплой
vercel --prod
```

---

## 🔍 ЧТО ПРОИСХОДИТ НА VERCEL

### Build Process:

```
1. Vercel клонирует репозиторий
   ↓
2. npm install (устанавливает зависимости)
   ↓
3. npm run build
   ↓
4. Next.js читает CSV из public/data/
   ├── procedures.csv
   ├── nhs_waits.csv
   ├── private_costs.csv
   └── clinics.csv
   ↓
5. Генерирует статические страницы:
   ├── / (homepage)
   ├── /procedures
   ├── /procedures/cataract
   ├── /procedures/hip
   ├── /procedures/knee
   ├── /comparison/cataract/london
   ├── /comparison/cataract/manchester
   └── ... (всего 28 страниц)
   ↓
6. Все данные встроены в HTML
   ↓
7. Деплой на CDN Vercel
```

### Runtime (после деплоя):

- ✅ Все страницы статические HTML
- ✅ Данные уже в HTML (не нужно читать CSV)
- ✅ Быстрая загрузка через CDN
- ✅ Нет запросов к файловой системе

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### ✅ Что работает:

1. **CSV файлы в `public/data/`** - ✅ Копируются в build
2. **`fs.readFileSync`** - ✅ Работает на build time
3. **Static Site Generation (SSG)** - ✅ Генерирует статические страницы
4. **`generateStaticParams`** - ✅ Создает все комбинации страниц

### ❌ Что НЕ работает:

1. **Чтение CSV в runtime** - ❌ Не нужно (данные уже в HTML)
2. **API routes для CSV** - ❌ Не нужно (SSG)
3. **Динамическое обновление данных** - ❌ Требует rebuild

---

## 🔄 ОБНОВЛЕНИЕ ДАННЫХ

### После обновления CSV файлов:

1. **Обновите файлы в `public/data/`:**
   ```bash
   # Замените файлы
   cp data/processed/nhs_waits.csv public/data/nhs_waits.csv
   cp data/processed/private_costs.csv public/data/private_costs.csv
   cp data/processed/clinics.csv public/data/clinics.csv
   ```

2. **Commit и push:**
   ```bash
   git add public/data/*.csv
   git commit -m "Update data files"
   git push
   ```

3. **Vercel автоматически:**
   - Обнаружит изменения
   - Запустит новый build
   - Прочитает обновленные CSV
   - Перегенерирует все страницы
   - Задеплоит новую версию

### Автоматическое обновление (будущее):

Когда настроите GitHub Actions (см. `firecrawl-cursor-automation.md`):
- Каждый понедельник GitHub Actions обновляет CSV
- Автоматически делает commit
- Vercel автоматически делает rebuild
- Данные всегда актуальные

---

## 🧪 ТЕСТИРОВАНИЕ ПЕРЕД ДЕПЛОЕМ

### 1. Локальная сборка:

```bash
# Соберите проект
npm run build

# Проверьте что нет ошибок
# Должно быть: "Generating static pages (28/28)"
```

### 2. Проверка production build:

```bash
# Запустите production сервер
npm run start

# Откройте http://localhost:3000
# Проверьте что:
# - Все страницы загружаются
# - Данные отображаются
# - Нет ошибок в консоли
```

### 3. Проверка CSV файлов:

```bash
# Убедитесь что все файлы на месте
ls -la public/data/*.csv

# Проверьте что файлы не пустые
wc -l public/data/*.csv
```

---

## 📊 ПРИМЕР: КАК ДАННЫЕ ВСТРАИВАЮТСЯ

### До build (CSV файл):
```csv
procedure_id,city,nhs_trust,avg_wait_weeks,date,source
cataract,London,Imperial College Healthcare NHS Trust,18,2025-11-05,My Planned Care
```

### После build (в HTML):
```html
<!-- Статическая страница /comparison/cataract/london -->
<h1>Cataract Surgery in London: NHS vs Private</h1>
<div>
  <p>NHS Wait: 18 weeks</p>
  <p>Private Cost: £2,500 - £3,500</p>
</div>
<!-- Данные уже встроены в HTML -->
```

---

## ✅ ИТОГОВЫЙ ОТВЕТ

**ДА, данные будут загружаться из CSV файлов на Vercel!**

### Как это работает:

1. ✅ CSV файлы в `public/data/` копируются в build
2. ✅ Next.js читает их через `fs.readFileSync` на build time
3. ✅ Генерирует статические HTML страницы с данными
4. ✅ После деплоя данные уже встроены в HTML
5. ✅ Быстрая загрузка через CDN Vercel

### Преимущества:

- ✅ Нет запросов к файлам в runtime
- ✅ Быстрая загрузка (статический HTML)
- ✅ SEO-friendly (данные в HTML)
- ✅ Бесплатный хостинг (Vercel free tier)

---

## 🚀 ГОТОВО К ДЕПЛОЮ!

Проект полностью готов к деплою на Vercel. Все данные будут работать корректно.

**Следующий шаг:** Деплой на Vercel! 🎉

