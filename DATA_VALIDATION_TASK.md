# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: ВАЛИДАЦИЯ ДАННЫХ ДЛЯ COMPARE THE WAIT

## 📋 ОБЩАЯ ИНФОРМАЦИЯ

**Проект:** Compare The Wait  
**Задача:** Полная валидация всех данных проекта  
**Формат отчета:** Excel/CSV с детальным описанием всех найденных проблем  
**Срок:** [Указать срок]  

---

## 📁 ФАЙЛЫ ДЛЯ ПРОВЕРКИ

### Основные файлы данных (обязательные):

1. **`data/processed/procedures.csv`**
2. **`data/processed/nhs_waits.csv`**
3. **`data/processed/private_costs.csv`**
4. **`data/processed/clinics.csv`**
5. **`public/data/cities.csv`**
6. **`data/raw/comparethewait-data-source.json`**

### Дополнительные файлы (если есть):

7. **`public/data/faq.csv`**
8. **`public/data/content.csv`**

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ ПО ФАЙЛАМ

### 1. ФАЙЛ: `data/processed/procedures.csv`

#### Структура файла:
- **Колонки:** `procedure_id`, `name`, `specialty`, `description`, `nhs_code`
- **Ожидаемое количество строк:** 3 (ровно 3 процедуры)
- **Кодировка:** UTF-8
- **Разделитель:** запятая (,)

#### Что проверить:

**1.1. Обязательные процедуры:**
- [ ] Присутствует процедура с `procedure_id = "cataract"`
- [ ] Присутствует процедура с `procedure_id = "hip"`
- [ ] Присутствует процедура с `procedure_id = "knee"`
- [ ] Нет дубликатов `procedure_id`
- [ ] Нет лишних процедур (только 3 строки)

**1.2. Валидация полей для каждой процедуры:**

**Для `cataract`:**
- [ ] `procedure_id` = "cataract" (точно, lowercase)
- [ ] `name` содержит "Cataract" или "Cataract Surgery"
- [ ] `specialty` = "Ophthalmology" (или вариация)
- [ ] `description` не пустое, минимум 20 символов
- [ ] `nhs_code` не пустое (формат: буквы + цифры)

**Для `hip`:**
- [ ] `procedure_id` = "hip" (точно, lowercase)
- [ ] `name` содержит "Hip" и "Replacement"
- [ ] `specialty` = "Orthopedics" (или вариация)
- [ ] `description` не пустое, минимум 20 символов
- [ ] `nhs_code` не пустое

**Для `knee`:**
- [ ] `procedure_id` = "knee" (точно, lowercase)
- [ ] `name` содержит "Knee" и "Replacement"
- [ ] `specialty` = "Orthopedics" (или вариация)
- [ ] `description` не пустое, минимум 20 символов
- [ ] `nhs_code` не пустое

**1.3. Формат данных:**
- [ ] Нет пустых значений (NULL, пустые строки)
- [ ] Нет пробелов в начале/конце значений
- [ ] Нет специальных символов, которые могут сломать CSV парсинг
- [ ] Все значения в кавычках, если содержат запятые

---

### 2. ФАЙЛ: `data/processed/nhs_waits.csv`

#### Структура файла:
- **Колонки:** `procedure_id`, `city`, `nhs_trust`, `avg_wait_weeks`, `date`, `source`
- **Ожидаемое количество строк:** 15 (3 процедуры × 5 городов)
- **Кодировка:** UTF-8
- **Разделитель:** запятая (,)

#### Что проверить:

**2.1. Полнота данных:**
- [ ] Всего 15 строк (ровно)
- [ ] Для каждой комбинации `procedure_id` × `city` есть ровно 1 запись
- [ ] Нет дубликатов комбинаций `procedure_id` + `city`

**2.2. Валидация `procedure_id`:**
- [ ] Только значения: "cataract", "hip", "knee"
- [ ] Каждый `procedure_id` встречается ровно 5 раз (по одному на каждый город)
- [ ] Нет опечаток (cataracts, hips, knees - неправильно)

**2.3. Валидация `city`:**
- [ ] Только значения: "London", "Manchester", "Birmingham", "Leeds", "Bristol"
- [ ] Каждый город встречается ровно 3 раза (по одному на каждую процедуру)
- [ ] Правильное написание (с заглавной буквы)
- [ ] Нет опечаток (Londom, Manchaster - неправильно)

**2.4. Валидация `nhs_trust`:**
- [ ] Не пустое значение
- [ ] Содержит "NHS" или "NHS Trust" или "NHS Foundation Trust"
- [ ] Минимум 10 символов
- [ ] Реальное название NHS Trust (можно проверить на сайте NHS)

**2.5. Валидация `avg_wait_weeks`:**
- [ ] Целое число (без десятичных знаков)
- [ ] Диапазон: от 0 до 200 недель (реалистичные значения)
- [ ] Для cataract: обычно 15-50 недель
- [ ] Для hip: обычно 18-80 недель
- [ ] Для knee: обычно 18-80 недель
- [ ] Нет отрицательных значений
- [ ] Нет нулевых значений (0 недель - подозрительно, но возможно)

**2.6. Валидация `date`:**
- [ ] Формат: YYYY-MM-DD (например: 2025-01-15)
- [ ] Валидная дата (не 32 января, не 13 месяц)
- [ ] Дата не в будущем (не позже сегодняшней даты)
- [ ] Дата не старше 2 лет (данные должны быть актуальными)
- [ ] Все даты одинаковые или близкие (данные обновлены одновременно)

**2.7. Валидация `source`:**
- [ ] Не пустое значение
- [ ] Обычно содержит "My Planned Care" или "NHS" или название источника
- [ ] Минимум 5 символов

**2.8. Логические проверки:**
- [ ] Для одного города разные процедуры могут иметь разные wait times
- [ ] Для одной процедуры в разных городах wait times могут отличаться
- [ ] Если есть данные за разные даты, проверить логику (более свежие данные должны быть актуальнее)

---

### 3. ФАЙЛ: `data/processed/private_costs.csv`

#### Структура файла:
- **Колонки:** `procedure_id`, `city`, `cost_min`, `cost_max`, `clinic_count`, `date`, `source`
- **Ожидаемое количество строк:** 15 (3 процедуры × 5 городов)
- **Кодировка:** UTF-8
- **Разделитель:** запятая (,)

#### Что проверить:

**3.1. Полнота данных:**
- [ ] Всего 15 строк (ровно)
- [ ] Для каждой комбинации `procedure_id` × `city` есть ровно 1 запись
- [ ] Нет дубликатов комбинаций `procedure_id` + `city`

**3.2. Валидация `procedure_id` и `city`:**
- [ ] Те же проверки, что и для `nhs_waits.csv` (раздел 2.2 и 2.3)

**3.3. Валидация `cost_min`:**
- [ ] Целое число (без десятичных знаков, без символа £)
- [ ] Положительное число (больше 0)
- [ ] Реалистичные диапазоны:
  - **Cataract:** £1,200 - £4,000
  - **Hip:** £9,500 - £16,000
  - **Knee:** £10,000 - £16,000
- [ ] `cost_min` всегда меньше или равен `cost_max`

**3.4. Валидация `cost_max`:**
- [ ] Целое число (без десятичных знаков, без символа £)
- [ ] Положительное число (больше 0)
- [ ] Реалистичные диапазоны (см. выше)
- [ ] `cost_max` всегда больше или равен `cost_min`
- [ ] Разница между `cost_max` и `cost_min` не более 50% от `cost_min` (разумный диапазон)

**3.5. Валидация `clinic_count`:**
- [ ] Целое число (больше 0)
- [ ] Диапазон: от 1 до 50 (реалистичное количество клиник)
- [ ] Для London: обычно 10-30 клиник
- [ ] Для других городов: обычно 3-15 клиник
- [ ] Соответствует количеству записей в `clinics.csv` для этой комбинации (см. раздел 4)

**3.6. Валидация `date`:**
- [ ] Те же проверки, что и для `nhs_waits.csv` (раздел 2.6)

**3.7. Валидация `source`:**
- [ ] Не пустое значение
- [ ] Обычно содержит "PHIN" или "clinic websites" или название источника
- [ ] Минимум 5 символов

**3.8. Логические проверки:**
- [ ] Для одной процедуры в разных городах цены могут отличаться (London обычно дороже)
- [ ] Для разных процедур в одном городе цены должны отличаться (hip/knee дороже cataract)
- [ ] `clinic_count` должен соответствовать количеству реальных клиник в `clinics.csv`

---

### 4. ФАЙЛ: `data/processed/clinics.csv`

#### Структура файла:
- **Колонки:** `clinic_id`, `name`, `city`, `procedure_id`, `price`, `url`, `phone`, `address`, `rating_stars`, `rating_count`, `cqc_rating`, `hospital_group`, `last_updated`, `details_last_updated`
- **Ожидаемое количество строк:** 45-75 (3-5 клиник на каждую из 15 комбинаций)
- **Кодировка:** UTF-8
- **Разделитель:** запятая (,)

#### Что проверить:

**4.1. Полнота данных:**
- [ ] Минимум 45 строк (3 клиники × 15 комбинаций)
- [ ] Для каждой комбинации `procedure_id` × `city` есть минимум 3 клиники
- [ ] Нет дубликатов `clinic_id`

**4.2. Валидация `clinic_id`:**
- [ ] Уникальное значение (нет дубликатов)
- [ ] Формат: lowercase, подчеркивания вместо пробелов
- [ ] Содержит название клиники, город и процедуру
- [ ] Примеры правильных форматов:
  - `moorfields_london_cataract`
  - `spire_manchester_hip`
  - `circle_birmingham_knee`
- [ ] Нет специальных символов (только буквы, цифры, подчеркивания)

**4.3. Валидация `name`:**
- [ ] Не пустое значение
- [ ] Минимум 5 символов
- [ ] Реальное название клиники (можно проверить на сайте)
- [ ] Нет опечаток

**4.4. Валидация `city`:**
- [ ] Только значения: "London", "Manchester", "Birmingham", "Leeds", "Bristol"
- [ ] Правильное написание (с заглавной буквы)

**4.5. Валидация `procedure_id`:**
- [ ] Только значения: "cataract", "hip", "knee"

**4.6. Валидация `price`:**
- [ ] Целое число (без десятичных знаков, без символа £)
- [ ] Положительное число (больше 0)
- [ ] Реалистичные диапазоны (см. раздел 3.3)
- [ ] Соответствует диапазону в `private_costs.csv` для этой комбинации
- [ ] Для одной комбинации `procedure_id` × `city` цены могут отличаться (разные клиники)

**4.7. Валидация `url`:**
- [ ] Если заполнено, то полный URL с `https://` или `http://`
- [ ] Валидный формат URL (можно проверить регулярным выражением)
- [ ] URL ведет на реальный сайт (можно проверить доступность)
- [ ] Нет опечаток в доменах

**4.8. Валидация `phone`:**
- [ ] Если заполнено, то UK формат телефона:
  - `020-xxxx-xxxx` (London)
  - `0161-xxx-xxxx` (Manchester)
  - `0121-xxx-xxxx` (Birmingham)
  - Или международный: `+44-xxx-xxx-xxxx`
- [ ] Минимум 10 цифр
- [ ] Нет букв (только цифры, дефисы, пробелы, скобки)

**4.9. Валидация `address`:**
- [ ] Если заполнено, то содержит адрес (минимум 10 символов)
- [ ] Обычно содержит название улицы, номер, город
- [ ] Соответствует `city` в той же строке

**4.10. Валидация `rating_stars`:**
- [ ] Если заполнено, то число от 0 до 5
- [ ] Может быть с одним знаком после запятой (например: 4.5)
- [ ] Если есть `rating_stars`, то должен быть `rating_count`

**4.11. Валидация `rating_count`:**
- [ ] Если заполнено, то целое положительное число
- [ ] Обычно от 1 до 10000
- [ ] Если есть `rating_count`, то должен быть `rating_stars`

**4.12. Валидация `cqc_rating`:**
- [ ] Если заполнено, то одно из значений:
  - "Outstanding"
  - "Good"
  - "Requires improvement"
  - "Inadequate"
- [ ] Правильное написание (с заглавной буквы)

**4.13. Валидация `hospital_group`:**
- [ ] Если заполнено, то известное название группы:
  - "Spire"
  - "Circle"
  - "Nuffield"
  - "BMI"
  - "Ramsay"
  - Или другое реальное название
- [ ] Правильное написание

**4.14. Валидация `last_updated`:**
- [ ] Формат: YYYY-MM-DD
- [ ] Валидная дата
- [ ] Дата не в будущем
- [ ] Дата не старше 1 года (данные должны быть актуальными)

**4.15. Валидация `details_last_updated`:**
- [ ] Если заполнено, то формат: YYYY-MM-DD
- [ ] Валидная дата
- [ ] Дата не в будущем
- [ ] Обычно равна или новее `last_updated`

**4.16. Логические проверки:**
- [ ] Для каждой комбинации `procedure_id` × `city` есть минимум 3 клиники
- [ ] Цены клиник в диапазоне `cost_min` - `cost_max` из `private_costs.csv`
- [ ] Количество клиник соответствует `clinic_count` в `private_costs.csv`
- [ ] Клиники отсортированы по цене (от меньшей к большей) для каждой комбинации

---

### 5. ФАЙЛ: `public/data/cities.csv`

#### Структура файла:
- **Колонки:** `city`, `slug`, `elderly_population`, `priority`
- **Ожидаемое количество строк:** 5 (ровно 5 городов)
- **Кодировка:** UTF-8
- **Разделитель:** запятая (,)

#### Что проверить:

**5.1. Полнота данных:**
- [ ] Всего 5 строк (ровно)
- [ ] Нет дубликатов `city`
- [ ] Нет дубликатов `slug`

**5.2. Валидация `city`:**
- [ ] Только значения: "London", "Manchester", "Birmingham", "Leeds", "Bristol"
- [ ] Правильное написание (с заглавной буквы)
- [ ] Соответствует значениям в `nhs_waits.csv` и `private_costs.csv`

**5.3. Валидация `slug`:**
- [ ] Уникальное значение
- [ ] Формат: lowercase, дефисы вместо пробелов
- [ ] Соответствует `city`:
  - London → "london"
  - Manchester → "manchester"
  - Birmingham → "birmingham"
  - Leeds → "leeds"
  - Bristol → "bristol"
- [ ] Нет специальных символов (только буквы, дефисы)

**5.4. Валидация `elderly_population`:**
- [ ] Если заполнено, то строка с числом (например: "150000" или "150,000")
- [ ] Реалистичные значения:
  - London: ~1,500,000
  - Manchester: ~200,000
  - Birmingham: ~250,000
  - Leeds: ~180,000
  - Bristol: ~150,000

**5.5. Валидация `priority`:**
- [ ] Если заполнено, то строка
- [ ] Обычно содержит приоритет запуска (например: "START HERE" или "Phase 1")

---

### 6. ФАЙЛ: `data/raw/comparethewait-data-source.json`

#### Структура файла:
- **Формат:** JSON
- **Кодировка:** UTF-8
- **Валидный JSON:** файл должен парситься без ошибок

#### Что проверить:

**6.1. Структура JSON:**
- [ ] Файл является валидным JSON (можно проверить JSON validator)
- [ ] Нет синтаксических ошибок
- [ ] Правильная структура (соответствует схеме из `lib/types/waitingTimes.ts`)

**6.2. Метаданные (`metadata`):**
- [ ] Присутствует объект `metadata`
- [ ] `source` - не пустая строка
- [ ] `data_version` - не пустая строка
- [ ] `last_updated` - дата в формате YYYY-MM-DD или ISO 8601
- [ ] `research_date` - дата в формате YYYY-MM-DD или ISO 8601
- [ ] `research_sources` - массив строк (не пустой)
- [ ] `disclaimer` - не пустая строка
- [ ] `data_quality_notes` - строка (может быть пустой)

**6.3. UK-wide данные (`uk_wide`):**
- [ ] Присутствует объект `uk_wide`
- [ ] Присутствуют все 3 процедуры:
  - `cataract` (тип: `UKWideProcedureData`)
  - `hip_replacement` (тип: `UKWideProcedureData`)
  - `knee_replacement` (тип: `UKWideProcedureData`)

**6.4. Валидация `uk_wide.cataract`:**
- [ ] `procedure_id` = "cataract"
- [ ] `procedure_name` содержит "Cataract"
- [ ] `average_age_at_procedure` - строка (например: "75 years")
- [ ] `annual_procedures_uk` - положительное число
- [ ] `waiting_times` - объект со всеми полями:
  - `official_nhs_target_weeks` - число от 0 до 52
  - `healthcare_provider_average_weeks` - положительное число
  - `patient_reported_average_weeks` - положительное число
  - `patient_reported_median_weeks` - положительное число
  - `patient_reported_min_weeks` - положительное число
  - `patient_reported_max_weeks` - положительное число (больше min)
  - `patient_reported_sample_size` - положительное целое число
- [ ] `private_comparison` - объект:
  - `average_wait_weeks` - число от 1 до 4 (обычно 1-2)
  - `average_cost_pounds` - положительное число
  - `cost_range_min_pounds` - положительное число (меньше или равно average_cost)
  - `cost_range_max_pounds` - положительное число (больше или равно average_cost)
- [ ] `key_facts` - массив строк (не пустой)
- [ ] `cancellation_risk` - объект:
  - `percentage_experience` - число от 0 до 100
  - `average_delay_weeks` - положительное число
  - `cancellations_per_patient_average` - положительное число

**6.5. Валидация `uk_wide.hip_replacement`:**
- [ ] Те же проверки, что и для `cataract` (раздел 6.4)
- [ ] `procedure_id` = "hip"
- [ ] `procedure_name` содержит "Hip"

**6.6. Валидация `uk_wide.knee_replacement`:**
- [ ] Те же проверки, что и для `cataract` (раздел 6.4)
- [ ] `procedure_id` = "knee"
- [ ] `procedure_name` содержит "Knee"

**6.7. Региональные данные (`regions`):**
- [ ] Присутствует массив `regions`
- [ ] Массив содержит ровно 5 регионов
- [ ] Каждый регион имеет `region_id` из списка: "london", "manchester", "birmingham", "leeds", "bristol"

**6.8. Валидация каждого региона:**
- [ ] `region_id` - уникальное значение (нет дубликатов)
- [ ] `region_name` - строка (например: "London")
- [ ] `nhs_trust_count` - положительное целое число
- [ ] `population_55_plus` - положительное число
- [ ] `cataract` - объект типа `RegionalProcedureData`:
  - `official_target_weeks` - число от 0 до 52
  - `patient_reported_average_weeks` - число или null (если null, то должно быть > 0 или null)
  - `patient_reported_median_weeks` - число или null
  - `patient_reported_range` - строка (например: "25-35 weeks")
  - `sample_size` - целое число (может быть 0)
  - `confidence` - одно из: "high", "medium", "low", "very_low", "no_data"
  - `private_average_weeks` - положительное число (обычно 1-2)
  - `private_average_cost_pounds` - положительное число
  - `recommended_display` - строка (не пустая)
- [ ] `hip` - объект типа `RegionalProcedureData` (те же проверки)
- [ ] `knee` - объект типа `RegionalProcedureData` (те же проверки)
- [ ] `key_insight` - строка (не пустая)

**6.9. Логические проверки:**
- [ ] Для каждой процедуры в регионах `patient_reported_average_weeks` должен быть >= 0 или null
- [ ] Если `sample_size` = 0, то `confidence` обычно "no_data" или "very_low"
- [ ] Если `sample_size` > 50, то `confidence` обычно "high" или "medium"
- [ ] `private_average_cost_pounds` в регионах соответствует диапазонам в `private_costs.csv`
- [ ] `recommended_display` содержит реалистичные значения (например: "25-35 weeks", не "0 weeks")

**6.10. Private clinics benchmark:**
- [ ] Присутствует объект `private_clinics_benchmark`
- [ ] `average_wait_days` - положительное число (обычно 7-14)
- [ ] `average_wait_weeks` - положительное число (обычно 1-2)
- [ ] `guaranteed_slot` - boolean
- [ ] `cancellation_rate_percent` - число от 0 до 100
- [ ] `cataract_average_cost` - положительное число
- [ ] `hip_average_cost` - положительное число
- [ ] `knee_average_cost` - положительное число
- [ ] `key_feature` - строка (не пустая)

**6.11. Messaging guidelines:**
- [ ] Присутствует объект `messaging_guidelines`
- [ ] `tone` - строка (не пустая)
- [ ] `avoid` - массив строк
- [ ] `key_phrases` - массив строк (не пустой)
- [ ] `british_spellings` - массив строк

---

### 7. ФАЙЛ: `public/data/faq.csv` (если существует)

#### Структура файла:
- **Колонки:** `faq_id`, `question`, `answer`, `procedure_id`, `category`
- **Кодировка:** UTF-8
- **Разделитель:** запятая (,)

#### Что проверить:

**7.1. Общая структура:**
- [ ] Файл существует
- [ ] Валидный CSV формат
- [ ] Нет пустых строк

**7.2. Валидация полей:**
- [ ] `faq_id` - уникальное значение
- [ ] `question` - не пустая строка, минимум 10 символов
- [ ] `answer` - не пустая строка, минимум 20 символов
- [ ] `procedure_id` - одно из: "cataract", "hip", "knee", "general"
- [ ] `category` - одно из: "general", "procedure", "extended"

**7.3. Логические проверки:**
- [ ] Для каждой процедуры есть минимум 3 FAQ
- [ ] Есть общие FAQ (`procedure_id = "general"`)

---

### 8. ФАЙЛ: `public/data/content.csv` (если существует)

#### Структура файла:
- **Колонки:** `content_id`, `section`, `key`, `value`, `language`
- **Кодировка:** UTF-8
- **Разделитель:** запятая (,)

#### Что проверить:

**8.1. Общая структура:**
- [ ] Файл существует
- [ ] Валидный CSV формат
- [ ] Нет пустых строк

**8.2. Валидация полей:**
- [ ] `content_id` - уникальное значение
- [ ] `section` - не пустая строка
- [ ] `key` - не пустая строка
- [ ] `value` - не пустая строка
- [ ] `language` - обычно "en" или "en-GB"

---

## 🔗 МЕЖФАЙЛОВЫЕ ПРОВЕРКИ

### Связи между файлами:

**9.1. Согласованность процедур:**
- [ ] Все `procedure_id` в `nhs_waits.csv` присутствуют в `procedures.csv`
- [ ] Все `procedure_id` в `private_costs.csv` присутствуют в `procedures.csv`
- [ ] Все `procedure_id` в `clinics.csv` присутствуют в `procedures.csv`
- [ ] Все процедуры в `comparethewait-data-source.json` соответствуют `procedures.csv`

**9.2. Согласованность городов:**
- [ ] Все `city` в `nhs_waits.csv` присутствуют в `cities.csv`
- [ ] Все `city` в `private_costs.csv` присутствуют в `cities.csv`
- [ ] Все `city` в `clinics.csv` присутствуют в `cities.csv`
- [ ] Все регионы в `comparethewait-data-source.json` соответствуют городам в `cities.csv`

**9.3. Согласованность цен:**
- [ ] Для каждой комбинации `procedure_id` × `city`:
  - Минимальная цена в `clinics.csv` >= `cost_min` в `private_costs.csv`
  - Максимальная цена в `clinics.csv` <= `cost_max` в `private_costs.csv`
  - Количество клиник в `clinics.csv` = `clinic_count` в `private_costs.csv`

**9.4. Согласованность времени ожидания:**
- [ ] Для каждой комбинации `procedure_id` × `city`:
  - `avg_wait_weeks` в `nhs_waits.csv` соответствует региональным данным в `comparethewait-data-source.json` (если есть)
  - Значения реалистичны (не 0, если это не специальный случай)

**9.5. Согласованность дат:**
- [ ] Все даты в файлах не старше 1 года
- [ ] Даты обновления близки друг к другу (данные собраны примерно в одно время)

---

## 📊 ФОРМАТ ОТЧЕТА

### Структура отчета:

Создать Excel файл или CSV с следующими колонками:

1. **File** - название файла
2. **Row/Line** - номер строки (если применимо) или путь к полю (для JSON)
3. **Field** - название поля/колонки
4. **Issue Type** - тип проблемы:
   - "Missing data" - отсутствующие данные
   - "Invalid format" - неверный формат
   - "Out of range" - значение вне допустимого диапазона
   - "Logic error" - логическая ошибка
   - "Inconsistency" - несогласованность между файлами
   - "Typo" - опечатка
5. **Current Value** - текущее значение (если есть)
6. **Expected Value** - ожидаемое значение
7. **Severity** - серьезность:
   - "Critical" - блокирует работу приложения
   - "High" - серьезная проблема, влияет на функциональность
   - "Medium" - средняя проблема, может вызвать ошибки
   - "Low" - незначительная проблема, косметическая
8. **Description** - подробное описание проблемы
9. **Recommendation** - рекомендация по исправлению

### Пример отчета:

| File | Row/Line | Field | Issue Type | Current Value | Expected Value | Severity | Description | Recommendation |
|------|----------|-------|------------|---------------|----------------|----------|-------------|----------------|
| nhs_waits.csv | 5 | city | Typo | "Londom" | "London" | High | Опечатка в названии города | Исправить на "London" |
| private_costs.csv | 12 | cost_min | Out of range | 500 | 9500-16000 | Critical | Цена слишком низкая для hip replacement | Проверить источник данных |
| clinics.csv | 23 | clinic_id | Invalid format | "clinic 1" | "clinic_1_london_cataract" | Medium | Неверный формат ID | Использовать формат: {name}_{city}_{procedure} |

---

## ✅ КРИТЕРИИ ПРИЕМКИ

Данные считаются валидированными, если:

1. ✅ Все обязательные файлы присутствуют
2. ✅ Все файлы имеют валидный формат (CSV парсится, JSON валиден)
3. ✅ Нет критических ошибок (Severity: Critical)
4. ✅ Количество строк соответствует ожидаемому
5. ✅ Все обязательные поля заполнены
6. ✅ Нет дубликатов ключевых полей
7. ✅ Все значения в допустимых диапазонах
8. ✅ Нет несогласованностей между файлами
9. ✅ Все даты актуальны (не старше 1 года)
10. ✅ Все URL и телефоны в правильном формате

---

## 📝 ПРИМЕЧАНИЯ

- Если какое-то поле помечено как опциональное (optional), его отсутствие не является ошибкой
- Для числовых полей допускаются небольшие отклонения от ожидаемых диапазонов, если они обоснованы
- При обнаружении проблем, которые требуют уточнения, указать их в отчете с пометкой "Требует уточнения"
- Если файл отсутствует, указать это в отчете как критическую проблему

---

## 📧 КОНТАКТЫ

При возникновении вопросов обращаться к: [Указать контакт]

---

**Дата создания ТЗ:** [Указать дату]  
**Версия:** 1.0  
**Статус:** Готово к выполнению

