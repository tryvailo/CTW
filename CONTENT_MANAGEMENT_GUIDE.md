# РУКОВОДСТВО ПО УПРАВЛЕНИЮ КОНТЕНТОМ

**Цель:** Объяснить, как обновлять контент сайта без изменения кода

---

## 📁 ФАЙЛЫ С КОНТЕНТОМ

### 1. FAQ (`public/data/faq.csv`)

Содержит все вопросы и ответы FAQ.

**Формат:**
```csv
faq_id,question,answer,procedure_id,category
general_1,Can I get surgery on the NHS?,Yes. If your condition...,general,general
cataract_1,Can I get cataract surgery...,Yes. If you have...,cataract,procedure
```

**Поля:**
- `faq_id` - уникальный идентификатор
- `question` - вопрос
- `answer` - ответ
- `procedure_id` - ID процедуры или "general" для общих вопросов
- `category` - "general" или "procedure"

**Как обновить:**
1. Откройте `public/data/faq.csv`
2. Найдите нужную строку
3. Измените `question` или `answer`
4. Сохраните файл
5. Пересоберите сайт: `npm run build`

---

### 2. Контент (`public/data/content.csv`)

Содержит весь текстовый контент для статических страниц и компонентов.

**Формат:**
```csv
content_id,section,key,value,language
value_prop_1,value_props,title_1,FREE,en
about_title,about,title,About Compare The Wait,en
```

**Поля:**
- `content_id` - уникальный идентификатор
- `section` - секция (value_props, about, privacy, terms и т.д.)
- `key` - ключ для доступа к тексту
- `value` - сам текст
- `language` - язык (по умолчанию "en")

**Как обновить:**
1. Откройте `public/data/content.csv`
2. Найдите нужную строку по `section` и `key`
3. Измените `value`
4. Сохраните файл
5. Пересоберите сайт: `npm run build`

---

## 🔍 КАК НАЙТИ НУЖНЫЙ ТЕКСТ

### Для FAQ:
1. Откройте `public/data/faq.csv`
2. Используйте поиск по вопросу или ответу
3. Или ищите по `procedure_id` (cataract, hip, knee, general)

### Для контента:
1. Откройте `public/data/content.csv`
2. Используйте поиск по:
   - `section` - название секции (about, privacy, terms, value_props)
   - `key` - ключ текста (title, mission_title, intro_text и т.д.)

**Примеры ключей:**
- `about_title` - заголовок страницы About
- `about_mission_text` - текст миссии
- `privacy_intro_text` - введение в Privacy Policy
- `value_prop_1` - первый value proposition

---

## 📝 ПРИМЕРЫ ОБНОВЛЕНИЯ

### Пример 1: Обновить FAQ вопрос

**Было:**
```csv
general_1,Can I get surgery on the NHS?,Yes. If your condition...,general,general
```

**Стало:**
```csv
general_1,Can I get surgery on the NHS?,Yes! If your condition meets NHS criteria, you can get FREE treatment on the NHS. You'll be referred by your GP or specialist...,general,general
```

### Пример 2: Обновить текст на странице About

**Было:**
```csv
about_mission_text,about,mission_text,"Compare The Wait helps people aged 65+...",en
```

**Стало:**
```csv
about_mission_text,about,mission_text,"Compare The Wait helps people aged 65+ understand their options for NHS vs private surgery. We provide honest, FREE comparisons of waiting times and costs so you can make an informed decision. Over 7.7 million people are waiting for NHS surgery right now!",en
```

---

## ✅ ПРОВЕРКА ИЗМЕНЕНИЙ

После обновления файлов:

1. **Проверьте синтаксис CSV:**
   - Все поля должны быть в кавычках, если содержат запятые
   - Заголовки должны быть на первой строке
   - Нет пустых строк между данными

2. **Пересоберите проект:**
   ```bash
   npm run build
   ```

3. **Проверьте в браузере:**
   - Запустите `npm run dev`
   - Откройте нужную страницу
   - Убедитесь, что текст обновился

---

## 🚨 ВАЖНО

1. **Всегда делайте резервную копию** перед изменением файлов
2. **Проверяйте синтаксис CSV** - ошибки могут сломать сайт
3. **Используйте кавычки** для текста с запятыми или переносами строк
4. **Не удаляйте заголовки** из CSV файлов
5. **Сохраняйте формат** - не меняйте структуру CSV

---

## 📊 СТРУКТУРА ФАЙЛОВ

```
public/data/
├── faq.csv          # Все FAQ вопросы и ответы
├── content.csv      # Весь текстовый контент
├── procedures.csv   # Процедуры (не меняйте без необходимости)
├── cities.csv       # Города (не меняйте без необходимости)
├── nhs_waits.csv    # NHS ожидания (обновляется автоматически)
├── private_costs.csv # Частные цены (обновляется автоматически)
└── clinics.csv      # Клиники (обновляется автоматически)
```

---

## 🔄 ПРОЦЕСС ОБНОВЛЕНИЯ

1. **Откройте нужный CSV файл**
2. **Найдите нужную строку** (используйте поиск)
3. **Обновите значение** в поле `value`, `question` или `answer`
4. **Сохраните файл**
5. **Проверьте синтаксис** CSV
6. **Пересоберите сайт:** `npm run build`
7. **Проверьте в браузере**

---

## 💡 СОВЕТЫ

- **Используйте редактор CSV** (Excel, Google Sheets, VS Code с расширением CSV)
- **Делайте изменения постепенно** - не меняйте все сразу
- **Проверяйте после каждого изменения**
- **Ведите changelog** - записывайте, что и когда изменили

---

## 📞 ПОДДЕРЖКА

Если возникли проблемы:
1. Проверьте синтаксис CSV
2. Убедитесь, что файл сохранен
3. Проверьте, что пересобрали сайт
4. Проверьте консоль браузера на ошибки

