# Анализ методики извлечения данных

## 📊 Текущий подход

### Что мы используем сейчас:

1. **API версия:** Firecrawl v1 (`app.v1.scrapeUrl()`)
2. **Формат данных:** `formats: ['html', 'markdown']`
3. **Метод извлечения:** Ручной парсинг через regex и простые парсеры

### Текущая реализация:

```javascript
// 1. Получаем HTML/Markdown
const result = await app.v1.scrapeUrl(url, {
  formats: ['html', 'markdown']
});

// 2. Ручной парсинг через regex
function parseNHSWaitingTimes(html, markdown, procedure, city) {
  const waitTimePatterns = [
    /(?:average|mean|median)\s+(?:wait|waiting)\s+(?:time|period)[:\s]+(\d+)\s+weeks?/i,
    /(\d+)\s+weeks?\s+(?:average|mean|median)/i,
    // ...
  ];
  // Поиск по паттернам...
}
```

### Проблемы текущего подхода:

❌ **Хрупкость:**
- Зависит от точной структуры HTML/Markdown
- Если сайт меняет структуру → парсер ломается
- Regex не понимает контекст

❌ **Сложность поддержки:**
- Нужно писать отдельные парсеры для каждого типа данных
- Сложно обрабатывать вариации форматов
- Требует глубокого знания структуры каждого сайта

❌ **Ограниченность:**
- Не может извлекать сложные структуры
- Плохо работает с динамическим контентом
- Не понимает семантику данных

---

## 🚀 JSON Mode (LLM Extract) - Альтернативный подход

### Что это:

**JSON Mode** использует LLM (Large Language Model) для извлечения структурированных данных по заданной схеме.

### Как работает:

1. **Определяем JSON Schema** - описываем структуру данных, которые хотим получить
2. **Отправляем запрос** с URL и схемой
3. **LLM анализирует страницу** и извлекает данные по схеме
4. **Получаем готовые структурированные данные** в JSON формате

### Пример использования (из документации):

```javascript
// Определяем схему данных
const schema = {
  type: "object",
  properties: {
    avg_wait_weeks: {
      type: "number",
      description: "Average waiting time in weeks"
    },
    nhs_trust: {
      type: "string",
      description: "Name of NHS Trust"
    },
    // ...
  }
};

// Извлекаем данные через LLM
const result = await app.scrape(url, {
  formats: [{
    type: "json",
    schema: schema
  }]
});

// Получаем готовые данные
const data = result.data.json;
```

### Преимущества JSON Mode:

✅ **Устойчивость к изменениям:**
- LLM понимает семантику, а не только структуру
- Работает даже если HTML структура меняется
- Адаптируется к вариациям форматов

✅ **Простота:**
- Не нужно писать парсеры
- Достаточно описать схему данных
- LLM сам найдет нужную информацию

✅ **Гибкость:**
- Может извлекать сложные структуры
- Понимает контекст
- Работает с динамическим контентом

✅ **Точность:**
- LLM лучше понимает, где находятся нужные данные
- Меньше ошибок парсинга
- Правильно обрабатывает edge cases

---

## 📋 Сравнение подходов

| Критерий | Текущий (Regex) | JSON Mode (LLM) |
|----------|----------------|-----------------|
| **Устойчивость** | ❌ Низкая (ломается при изменении структуры) | ✅ Высокая (понимает семантику) |
| **Простота** | ❌ Сложно (нужно писать парсеры) | ✅ Просто (только схема) |
| **Точность** | ⚠️ Средняя (зависит от качества regex) | ✅ Высокая (LLM понимает контекст) |
| **Скорость** | ✅ Быстро (простой парсинг) | ⚠️ Медленнее (LLM обработка) |
| **Стоимость** | ✅ Бесплатно/дешево | ⚠️ Дороже (использует LLM) |
| **Поддержка** | ❌ Сложная (нужно обновлять парсеры) | ✅ Простая (схема остается) |

---

## 🎯 Оценка пригодности для нашей задачи

### Наша задача:
- Извлечение NHS waiting times (числа, названия trusts)
- Извлечение private costs (диапазоны цен)
- Извлечение clinic details (названия, цены, контакты)

### Оценка JSON Mode: ⭐⭐⭐⭐⭐ (5/5) - ОТЛИЧНО ПОДХОДИТ

**Почему JSON Mode идеален для нас:**

1. **Структурированные данные:**
   - Мы точно знаем, какие поля нужны
   - Можно описать четкую схему
   - LLM легко извлечет нужные данные

2. **Нестабильные сайты:**
   - My Planned Care может менять структуру
   - PHIN имеет сложную динамическую структуру
   - JSON Mode справится лучше, чем regex

3. **Разнообразие форматов:**
   - Разные сайты показывают данные по-разному
   - LLM понимает вариации ("18 weeks", "18-week wait", "waiting: 18 weeks")
   - Regex требует множества паттернов

4. **Сложные структуры:**
   - Клиники могут быть в таблицах, списках, карточках
   - LLM найдет данные независимо от формата
   - Regex нужно писать для каждого формата

### Недостатки JSON Mode:

⚠️ **Стоимость:**
- Использует LLM → дороже чем простой scraping
- Но для 15 комбинаций (3 процедуры × 5 городов) это приемлемо

⚠️ **Скорость:**
- LLM обработка медленнее regex
- Но для еженедельного обновления это не критично

⚠️ **API версия:**
- JSON Mode требует v2 API
- Нужно проверить поддержку в нашем пакете

---

## 💡 Рекомендация

### ✅ РЕКОМЕНДУЮ перейти на JSON Mode

**Причины:**
1. **Надежность** - меньше сломается при изменении сайтов
2. **Простота** - не нужно поддерживать сложные парсеры
3. **Точность** - лучше извлекает данные
4. **Масштабируемость** - легко добавить новые поля

**План миграции:**
1. Проверить поддержку v2 API в пакете `firecrawl`
2. Создать JSON схемы для каждого типа данных
3. Заменить парсеры на JSON Mode вызовы
4. Протестировать на реальных данных
5. Оставить fallback на случай ошибок

**Гибридный подход (опционально):**
- Использовать JSON Mode как основной метод
- Оставить regex парсеры как fallback
- Если JSON Mode не работает → использовать regex

---

## 📝 Вывод

**Текущий подход (Regex):** ⚠️ Работает, но хрупкий и сложный в поддержке

**JSON Mode (LLM):** ✅ Идеально подходит для нашей задачи, стоит перейти

**Следующий шаг:** Проверить поддержку v2 API и создать прототип с JSON Mode

