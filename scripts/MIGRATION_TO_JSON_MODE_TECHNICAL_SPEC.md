# Техническое задание: Миграция на JSON Mode (LLM Extract)

**Версия:** 1.0  
**Дата:** 2025-11-07  
**Приоритет:** Высокий  
**Оценка времени:** 4-6 часов

---

## 📋 Обзор

Миграция с ручного парсинга (regex) на JSON Mode (LLM Extract) для повышения надежности и упрощения поддержки скрипта скрапинга данных.

**Цель:** Использовать LLM для извлечения структурированных данных вместо хрупких regex парсеров.

**Преимущества:**
- ✅ Устойчивость к изменениям структуры сайтов
- ✅ Простота поддержки (только JSON схемы)
- ✅ Высокая точность извлечения данных
- ✅ Меньше кода для поддержки

---

## 🎯 Задача 1: Создать JSON-схемы для каждого типа данных

**Оценка времени:** 1-1.5 часа  
**Приоритет:** Высокий

### Описание

Создать JSON схемы для трех типов данных:
1. NHS waiting times
2. Private costs
3. Clinic details

### Требования

#### 1.1 JSON схема для NHS Waiting Times

**Файл:** `scripts/schemas/nhs-waits-schema.js`

**Схема должна включать:**
- `avg_wait_weeks` (number) - среднее время ожидания в неделях
- `nhs_trust` (string) - название NHS Trust
- `percent_within_18_weeks` (number, optional) - процент в пределах 18 недель
- `procedure_name` (string) - название процедуры
- `city` (string) - город

**Prompt должен:**
- Указывать на поиск информации о времени ожидания
- Упоминать процедуру и город
- Просить найти NHS Trust name

**Чеклист:**

- [ ] Создать файл `scripts/schemas/nhs-waits-schema.js`
- [ ] Определить JSON Schema с правильными типами
- [ ] Добавить описания для каждого поля
- [ ] Создать prompt на английском языке
- [ ] Убедиться, что схема соответствует структуре CSV (`nhs_waits.csv`)
- [ ] Экспортировать схему и prompt как объект
- [ ] Добавить комментарии с примерами ожидаемых данных

**Критерии приемки:**
- ✅ Схема валидна (можно проверить через JSON Schema validator)
- ✅ Все обязательные поля из CSV присутствуют
- ✅ Prompt четко описывает, что нужно извлечь
- ✅ Схема экспортируется корректно

---

#### 1.2 JSON схема для Private Costs

**Файл:** `scripts/schemas/private-costs-schema.js`

**Схема должна включать:**
- `cost_min_gbp` (number) - минимальная стоимость в GBP
- `cost_max_gbp` (number) - максимальная стоимость в GBP
- `clinic_count` (number) - количество клиник
- `procedure_name` (string) - название процедуры
- `city` (string) - город

**Prompt должен:**
- Указывать на поиск ценовых диапазонов
- Просить найти минимальную и максимальную стоимость
- Упоминать подсчет количества клиник

**Чеклист:**

- [ ] Создать файл `scripts/schemas/private-costs-schema.js`
- [ ] Определить JSON Schema с правильными типами
- [ ] Добавить описания для каждого поля
- [ ] Создать prompt на английском языке
- [ ] Убедиться, что схема соответствует структуре CSV (`private_costs.csv`)
- [ ] Экспортировать схему и prompt как объект
- [ ] Добавить комментарии с примерами ожидаемых данных

**Критерии приемки:**
- ✅ Схема валидна
- ✅ Все обязательные поля из CSV присутствуют
- ✅ Prompt четко описывает извлечение цен
- ✅ Схема экспортируется корректно

---

#### 1.3 JSON схема для Clinic Details

**Файл:** `scripts/schemas/clinics-schema.js`

**Схема должна включать:**
- Массив объектов клиник
- Каждый объект содержит:
  - `clinic_name` (string) - название клиники
  - `price_gbp` (number) - цена в GBP
  - `phone_number` (string, optional) - телефон
  - `website_url` (string, optional) - веб-сайт
  - `address` (string, optional) - адрес

**Prompt должен:**
- Указывать на поиск списка клиник
- Просить извлечь до 5 клиник
- Упоминать необходимость найти контактную информацию

**Чеклист:**

- [ ] Создать файл `scripts/schemas/clinics-schema.js`
- [ ] Определить JSON Schema как массив объектов
- [ ] Добавить описания для каждого поля
- [ ] Создать prompt на английском языке
- [ ] Убедиться, что схема соответствует структуре CSV (`clinics.csv`)
- [ ] Экспортировать схему и prompt как объект
- [ ] Добавить комментарии с примерами ожидаемых данных

**Критерии приемки:**
- ✅ Схема валидна (массив объектов)
- ✅ Все обязательные поля из CSV присутствуют
- ✅ Prompt четко описывает извлечение списка клиник
- ✅ Схема экспортируется корректно

---

#### 1.4 Создать индексный файл для схем

**Файл:** `scripts/schemas/index.js`

**Чеклист:**

- [ ] Создать файл `scripts/schemas/index.js`
- [ ] Импортировать все схемы
- [ ] Экспортировать их как единый объект
- [ ] Добавить JSDoc комментарии

**Критерии приемки:**
- ✅ Все схемы доступны через один импорт
- ✅ Экспорт работает корректно

---

## 🔄 Задача 2: Обновить scraper.js для использования JSON Mode

**Оценка времени:** 2-3 часа  
**Приоритет:** Высокий

### Описание

Заменить функции парсинга на вызовы JSON Mode API, сохранив fallback логику.

### Требования

#### 2.1 Обновить импорты и зависимости

**Чеклист:**

- [ ] Импортировать схемы из `scripts/schemas/index.js`
- [ ] Убедиться, что используется `app.scrape()` (v2 API) вместо `app.v1.scrapeUrl()`
- [ ] Проверить, что все необходимые утилиты импортированы

**Критерии приемки:**
- ✅ Нет ошибок импорта
- ✅ Используется правильный API метод

---

#### 2.2 Создать функцию для извлечения NHS waits через JSON Mode

**Функция:** `extractNHSWaitsWithJSONMode(url, procedure, city)`

**Чеклист:**

- [ ] Создать функцию `extractNHSWaitsWithJSONMode()`
- [ ] Использовать схему из `schemas/nhs-waits-schema.js`
- [ ] Вызвать `app.scrape()` с JSON форматом
- [ ] Обработать результат и преобразовать в формат CSV
- [ ] Добавить обработку ошибок (try-catch)
- [ ] Логировать успешные и неуспешные попытки
- [ ] Вернуть данные в формате, совместимом с существующим кодом
- [ ] Добавить комментарии с описанием функции

**Критерии приемки:**
- ✅ Функция возвращает данные в правильном формате
- ✅ Обрабатываются ошибки корректно
- ✅ Логирование информативное
- ✅ Данные соответствуют структуре CSV

---

#### 2.3 Создать функцию для извлечения Private Costs через JSON Mode

**Функция:** `extractPHINCostsWithJSONMode(url, procedure, city)`

**Чеклист:**

- [ ] Создать функцию `extractPHINCostsWithJSONMode()`
- [ ] Использовать схему из `schemas/private-costs-schema.js`
- [ ] Вызвать `app.scrape()` с JSON форматом
- [ ] Обработать результат и преобразовать в формат CSV
- [ ] Добавить обработку ошибок (try-catch)
- [ ] Логировать успешные и неуспешные попытки
- [ ] Вернуть данные в формате, совместимом с существующим кодом
- [ ] Добавить комментарии с описанием функции

**Критерии приемки:**
- ✅ Функция возвращает данные в правильном формате
- ✅ Обрабатываются ошибки корректно
- ✅ Логирование информативное
- ✅ Данные соответствуют структуре CSV

---

#### 2.4 Создать функцию для извлечения Clinics через JSON Mode

**Функция:** `extractClinicsWithJSONMode(url, procedure, city)`

**Чеклист:**

- [ ] Создать функцию `extractClinicsWithJSONMode()`
- [ ] Использовать схему из `schemas/clinics-schema.js`
- [ ] Вызвать `app.scrape()` с JSON форматом
- [ ] Обработать массив клиник и преобразовать в формат CSV
- [ ] Генерировать `clinic_id` для каждой клиники
- [ ] Добавить обработку ошибок (try-catch)
- [ ] Логировать успешные и неуспешные попытки
- [ ] Вернуть массив данных в формате, совместимом с существующим кодом
- [ ] Добавить комментарии с описанием функции

**Критерии приемки:**
- ✅ Функция возвращает массив данных в правильном формате
- ✅ Обрабатываются ошибки корректно
- ✅ Логирование информативное
- ✅ Данные соответствуют структуре CSV
- ✅ `clinic_id` генерируется корректно

---

#### 2.5 Обновить функцию `scrapeNHSWaits()`

**Чеклист:**

- [ ] Заменить вызов `parseNHSWaitingTimes()` на `extractNHSWaitsWithJSONMode()`
- [ ] Сохранить fallback логику (если JSON Mode не работает)
- [ ] Обновить логирование для указания использования JSON Mode
- [ ] Добавить fallback на старый парсер (если JSON Mode вернул null/пусто)
- [ ] Убедиться, что структура данных совместима

**Критерии приемки:**
- ✅ JSON Mode используется как основной метод
- ✅ Fallback работает корректно
- ✅ Логирование понятное
- ✅ Данные в правильном формате

---

#### 2.6 Обновить функцию `scrapePHINData()`

**Чеклист:**

- [ ] Заменить вызов `parsePHINCosts()` на `extractPHINCostsWithJSONMode()`
- [ ] Заменить вызов `parsePHINClinics()` на `extractClinicsWithJSONMode()`
- [ ] Сохранить fallback логику для обоих типов данных
- [ ] Обновить логирование для указания использования JSON Mode
- [ ] Добавить fallback на старые парсеры (если JSON Mode вернул null/пусто)
- [ ] Убедиться, что структура данных совместима

**Критерии приемки:**
- ✅ JSON Mode используется как основной метод
- ✅ Fallback работает корректно
- ✅ Логирование понятное
- ✅ Данные в правильном формате

---

#### 2.7 Обновить функцию `scrapeWithRetry()` для поддержки JSON Mode

**Чеклист:**

- [ ] Проверить, что `scrapeWithRetry()` работает с `app.scrape()`
- [ ] Убедиться, что параметры передаются корректно
- [ ] Проверить обработку ошибок
- [ ] Обновить timeout значения (если нужно)

**Критерии приемки:**
- ✅ Retry логика работает с JSON Mode
- ✅ Ошибки обрабатываются корректно
- ✅ Timeout установлен правильно

---

#### 2.8 Удалить или закомментировать старые функции парсинга

**Чеклист:**

- [ ] Закомментировать `parseNHSWaitingTimes()` (не удалять, как fallback)
- [ ] Закомментировать `parsePHINCosts()` (не удалять, как fallback)
- [ ] Закомментировать `parsePHINClinics()` (не удалять, как fallback)
- [ ] Добавить комментарии, что это fallback функции
- [ ] Убедиться, что код компилируется без ошибок

**Критерии приемки:**
- ✅ Старые функции доступны как fallback
- ✅ Нет ошибок компиляции
- ✅ Комментарии понятные

---

## 🧪 Задача 3: Протестировать на реальных данных

**Оценка времени:** 1-1.5 часа  
**Приоритет:** Высокий

### Описание

Протестировать JSON Mode на реальных URL сайтов и убедиться, что данные извлекаются корректно.

### Требования

#### 3.1 Создать тестовый скрипт

**Файл:** `scripts/test-json-mode.js`

**Чеклист:**

- [ ] Создать файл `scripts/test-json-mode.js`
- [ ] Импортировать все функции извлечения
- [ ] Создать тестовые URL для каждого типа данных
- [ ] Добавить функцию для сравнения результатов
- [ ] Добавить вывод результатов в консоль
- [ ] Добавить обработку ошибок

**Критерии приемки:**
- ✅ Скрипт запускается без ошибок
- ✅ Тестирует все три типа данных
- [ ] Результаты выводятся понятно

---

#### 3.2 Тестирование NHS Waiting Times

**Чеклист:**

- [ ] Запустить тест для My Planned Care URL
- [ ] Проверить, что `avg_wait_weeks` извлекается корректно
- [ ] Проверить, что `nhs_trust` извлекается корректно
- [ ] Проверить, что данные соответствуют ожидаемому формату
- [ ] Проверить обработку ошибок (если сайт недоступен)
- [ ] Задокументировать результаты теста

**Критерии приемки:**
- ✅ Данные извлекаются корректно
- ✅ Формат данных правильный
- ✅ Ошибки обрабатываются

---

#### 3.3 Тестирование Private Costs

**Чеклист:**

- [ ] Запустить тест для PHIN URL
- [ ] Проверить, что `cost_min_gbp` извлекается корректно
- [ ] Проверить, что `cost_max_gbp` извлекается корректно
- [ ] Проверить, что `clinic_count` извлекается корректно
- [ ] Проверить, что данные соответствуют ожидаемому формату
- [ ] Проверить обработку ошибок (если сайт недоступен)
- [ ] Задокументировать результаты теста

**Критерии приемки:**
- ✅ Данные извлекаются корректно
- ✅ Формат данных правильный
- ✅ Ошибки обрабатываются

---

#### 3.4 Тестирование Clinic Details

**Чеклист:**

- [ ] Запустить тест для PHIN URL
- [ ] Проверить, что массив клиник извлекается
- [ ] Проверить, что каждая клиника имеет все обязательные поля
- [ ] Проверить, что `clinic_id` генерируется корректно
- [ ] Проверить, что данные соответствуют ожидаемому формату
- [ ] Проверить обработку ошибок (если сайт недоступен)
- [ ] Задокументировать результаты теста

**Критерии приемки:**
- ✅ Массив клиник извлекается корректно
- ✅ Все обязательные поля присутствуют
- ✅ Формат данных правильный
- ✅ Ошибки обрабатываются

---

#### 3.5 Интеграционное тестирование

**Чеклист:**

- [ ] Запустить полный скрипт `scraper.js` с JSON Mode
- [ ] Проверить, что все 15 комбинаций обрабатываются
- [ ] Проверить, что CSV файлы создаются корректно
- [ ] Проверить, что данные валидируются
- [ ] Сравнить результаты с предыдущими CSV (если есть)
- [ ] Проверить производительность (время выполнения)
- [ ] Задокументировать результаты

**Критерии приемки:**
- ✅ Все комбинации обрабатываются
- ✅ CSV файлы создаются с правильными данными
- ✅ Валидация проходит
- ✅ Производительность приемлемая

---

#### 3.6 Тестирование fallback логики

**Чеклист:**

- [ ] Симулировать ошибку JSON Mode (неверный API key)
- [ ] Проверить, что fallback на старые парсеры работает
- [ ] Проверить, что fallback на CSV файлы работает
- [ ] Проверить, что логирование указывает на использование fallback
- [ ] Задокументировать результаты

**Критерии приемки:**
- ✅ Fallback на парсеры работает
- ✅ Fallback на CSV работает
- ✅ Логирование понятное

---

## 🛡️ Задача 4: Оставить fallback на существующие CSV

**Оценка времени:** 30 минут  
**Приоритет:** Средний

### Описание

Убедиться, что fallback логика на существующие CSV файлы работает корректно и используется при ошибках JSON Mode.

### Требования

#### 4.1 Проверить функцию `getFallbackData()`

**Чеклист:**

- [ ] Проверить, что функция `getFallbackData()` работает корректно
- [ ] Убедиться, что она читает CSV файлы из правильных путей
- [ ] Проверить, что данные фильтруются по `procedure_id` и `city`
- [ ] Проверить, что дата обновляется на текущую
- [ ] Проверить обработку ошибок (если файл не существует)

**Критерии приемки:**
- ✅ Функция работает корректно
- ✅ Данные читаются правильно
- ✅ Фильтрация работает
- ✅ Дата обновляется

---

#### 4.2 Интегрировать fallback в функции извлечения

**Чеклист:**

- [ ] В `extractNHSWaitsWithJSONMode()` добавить fallback на `getFallbackData('nhs_waits')`
- [ ] В `extractPHINCostsWithJSONMode()` добавить fallback на `getFallbackData('private_costs')`
- [ ] В `extractClinicsWithJSONMode()` добавить fallback на `getFallbackData('clinics')`
- [ ] Убедиться, что fallback вызывается при ошибках
- [ ] Убедиться, что fallback вызывается при пустых результатах
- [ ] Добавить логирование использования fallback

**Критерии приемки:**
- ✅ Fallback интегрирован во все функции
- ✅ Fallback вызывается при ошибках
- ✅ Fallback вызывается при пустых результатах
- ✅ Логирование информативное

---

#### 4.3 Протестировать fallback сценарии

**Чеклист:**

- [ ] Протестировать fallback при ошибке API (неверный ключ)
- [ ] Протестировать fallback при пустом результате JSON Mode
- [ ] Протестировать fallback при отсутствии CSV файлов
- [ ] Проверить, что скрипт не падает при всех сценариях
- [ ] Задокументировать результаты

**Критерии приемки:**
- ✅ Fallback работает во всех сценариях
- ✅ Скрипт не падает
- ✅ Логирование понятное

---

#### 4.4 Обновить документацию

**Чеклист:**

- [ ] Обновить `SCRAPER_SETUP_COMPLETE.md` с информацией о JSON Mode
- [ ] Добавить описание fallback логики
- [ ] Обновить примеры использования
- [ ] Добавить информацию о стоимости JSON Mode

**Критерии приемки:**
- ✅ Документация актуальна
- ✅ Fallback логика описана
- ✅ Примеры работают

---

## 📊 Общий чеклист проекта

### Перед началом работы

- [ ] Прочитать техническое задание полностью
- [ ] Убедиться, что API key настроен
- [ ] Проверить, что все зависимости установлены
- [ ] Создать ветку для разработки (если используется Git)

### После завершения всех задач

- [ ] Все тесты проходят
- [ ] Код компилируется без ошибок
- [ ] Линтер не выдает ошибок
- [ ] Документация обновлена
- [ ] Код закоммичен и запушен
- [ ] GitHub Actions workflow работает (если настроен)

---

## 🐛 Известные проблемы и ограничения

### Проблемы

1. **Стоимость JSON Mode:**
   - Использование LLM дороже простого scraping
   - Для 15 комбинаций это приемлемо, но нужно мониторить

2. **Скорость:**
   - JSON Mode медленнее regex парсинга
   - Для еженедельного обновления это не критично

3. **Зависимость от LLM:**
   - Если LLM недоступен, нужен fallback
   - Fallback логика должна быть надежной

### Ограничения

- JSON Mode требует валидный API key
- Некоторые сайты могут блокировать запросы
- LLM может иногда возвращать неточные данные

---

## 📝 Примечания

- Старые функции парсинга не удаляются, а закомментируются как fallback
- Все изменения должны быть обратно совместимы
- Тестирование должно проводиться на реальных данных
- Документация должна обновляться параллельно с кодом

---

## ✅ Критерии успешного завершения

1. ✅ Все JSON схемы созданы и валидны
2. ✅ `scraper.js` использует JSON Mode как основной метод
3. ✅ Fallback логика работает корректно
4. ✅ Все тесты проходят
5. ✅ CSV файлы создаются с правильными данными
6. ✅ Документация обновлена
7. ✅ Код закоммичен и готов к использованию

---

**Статус:** Готово к выполнению  
**Ответственный:** Разработчик  
**Дата начала:** TBD  
**Дата завершения:** TBD

