# 📋 Как получить отчет по неуспешным кейсам

## 🚀 Тест запущен

Тест всех 49 URL запущен в фоне. Ожидаемое время выполнения: **5-10 минут**.

---

## 📊 Проверка прогресса

### Вариант 1: Проверить статус
```bash
node scripts/check-test-progress.js
```

### Вариант 2: Проверить файл результатов
```bash
ls -la scripts/test-results-all-treatmentconnect.json
```

Если файл существует - тест завершен.

---

## 📝 Генерация отчета

После завершения теста запустите:

```bash
# Полный детальный отчет (Markdown)
node scripts/generate-failure-report-final.js

# Быстрый анализ (консоль)
node scripts/analyze-failures-quick.js
```

### Созданные файлы:

1. **`scripts/FAILED_CASES_DETAILED_REPORT.md`**
   - Полный детальный отчет в Markdown
   - Все неудачные кейсы с URL, процедурами, запросами
   - Анализ по типам ошибок, процедурам, городам
   - Рекомендации

2. **`scripts/FAILED_CASES_SUMMARY.json`**
   - Краткая сводка в JSON
   - Статистика по ошибкам
   - Список всех неудачных URL

3. **`scripts/test-results-all-treatmentconnect.json`**
   - Полные результаты всех тестов
   - Успешные и неудачные кейсы
   - Детальная информация по каждому тесту

---

## 🔍 Что будет в отчете

### Для каждого неудачного кейса:

1. **URL** - полный URL который тестировался
2. **Процедура** - cataract, hip, или knee
3. **Город** - London, Manchester, Birmingham, Leeds, или Bristol
4. **Ошибка** - описание ошибки
5. **Использованный промпт** - полный промпт для Firecrawl JSON Mode
6. **Использованная схема** - JSON Schema которая использовалась
7. **Ответ от Firecrawl** - что вернул Firecrawl (если есть)
8. **Анализ сырых данных** - детальный анализ ответа

### Группировки:

- По типам ошибок
- По процедурам
- По городам
- По паттернам URL (больницам)

---

## ⏱️ Ожидаемое время

- **Тест:** 5-10 минут (49 URL × 6 секунд)
- **Генерация отчета:** < 1 секунды

---

## 📞 Если тест завис

Проверьте процесс:
```bash
ps aux | grep "test-all-treatmentconnect"
```

Если нужно перезапустить:
```bash
# Убить процесс
pkill -f test-all-treatmentconnect

# Запустить заново
export FIRECRAWL_API_KEY=fc-864a2c592f884561aa6887041fafcaf8
node scripts/test-all-treatmentconnect.js
```

