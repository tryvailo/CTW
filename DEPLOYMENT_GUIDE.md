# РУКОВОДСТВО ПО ДЕПЛОЮ

**Проект:** Compare The Wait  
**Платформа:** Vercel (рекомендуется)  
**Дата:** 2025-01-15

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Подготовка к деплою

#### Проверка перед деплоем:
```bash
# Проверить что все работает локально
npm run build
npm run start
```

#### Убедиться что:
- ✅ Все CSV файлы в `public/data/`
- ✅ Нет ошибок TypeScript (`npm run build`)
- ✅ Нет ошибок линтера (`npm run lint`)
- ✅ Все страницы генерируются

---

## 📦 ДЕПЛОЙ НА VERCEL

### Вариант 1: Через GitHub (рекомендуется)

1. **Создать GitHub репозиторий**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Compare The Wait MVP"
   git remote add origin https://github.com/yourusername/compare-the-wait.git
   git push -u origin main
   ```

2. **Подключить к Vercel**
   - Зайти на https://vercel.com
   - Нажать "New Project"
   - Импортировать GitHub репозиторий
   - Vercel автоматически определит Next.js

3. **Настроить переменные окружения**
   - В настройках проекта Vercel
   - Добавить переменную:
     ```
     NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app
     ```
   - Или если есть кастомный домен:
     ```
     NEXT_PUBLIC_BASE_URL=https://comparethewait.co.uk
     ```

4. **Деплой**
   - Vercel автоматически задеплоит при push в main
   - Или нажать "Deploy" вручную

### Вариант 2: Через Vercel CLI

```bash
# Установить Vercel CLI
npm i -g vercel

# Логин
vercel login

# Деплой
vercel

# Production деплой
vercel --prod
```

---

## ⚙️ НАСТРОЙКИ VERCEL

### Build Settings
- **Framework Preset:** Next.js
- **Build Command:** `npm run build` (по умолчанию)
- **Output Directory:** `.next` (по умолчанию)
- **Install Command:** `npm install` (по умолчанию)

### Environment Variables
```
NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app
```

### Build Settings (опционально)
- Node.js Version: 18.x или выше
- Enable Automatic HTTPS: ✅

---

## 🔍 ПОСЛЕ ДЕПЛОЯ

### 1. Проверка работы сайта

Проверить все страницы:
- ✅ `/` - Homepage
- ✅ `/comparison/cataract/london` - Comparison page
- ✅ `/procedures/cataract` - Procedure page
- ✅ `/faq` - FAQ page
- ✅ `/about` - About page
- ✅ `/privacy-policy` - Privacy page
- ✅ `/terms-of-service` - Terms page
- ✅ `/sitemap.xml` - Sitemap
- ✅ `/robots.txt` - Robots.txt

### 2. Google Search Console

1. **Добавить сайт**
   - Зайти на https://search.google.com/search-console
   - Добавить свойство (URL prefix)
   - Подтвердить владение (через HTML тег или DNS)

2. **Загрузить Sitemap**
   - Перейти в "Sitemaps"
   - Добавить: `https://your-domain.vercel.app/sitemap.xml`
   - Проверить статус индексации

3. **Мониторинг**
   - Проверять Coverage report
   - Мониторить индексацию страниц
   - Проверять Performance

### 3. Google Analytics (опционально)

1. **Создать аккаунт**
   - Зайти на https://analytics.google.com
   - Создать свойство для сайта

2. **Добавить tracking код**
   - Скопировать Measurement ID (G-XXXXXXXXXX)
   - Добавить в `app/layout.tsx`:
   ```tsx
   import Script from 'next/script'
   
   // В RootLayout:
   <Script
     src={`https://www.googletagmanager.com/gtag/js?id=${process.env.NEXT_PUBLIC_GA_ID}`}
     strategy="afterInteractive"
   />
   <Script id="google-analytics" strategy="afterInteractive">
     {`
       window.dataLayer = window.dataLayer || [];
       function gtag(){dataLayer.push(arguments);}
       gtag('js', new Date());
       gtag('config', '${process.env.NEXT_PUBLIC_GA_ID}');
     `}
   </Script>
   ```

3. **Добавить переменную окружения**
   ```
   NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
   ```

### 4. Lighthouse Audit

Проверить производительность:
```bash
# Установить Lighthouse CLI
npm i -g lighthouse

# Запустить аудит
lighthouse https://your-domain.vercel.app --view
```

Цели:
- Performance: 85+
- Accessibility: 95+
- Best Practices: 90+
- SEO: 95+

---

## 📊 МОНИТОРИНГ

### Vercel Analytics
- Vercel предоставляет базовую аналитику
- Можно подключить Vercel Analytics для детальной статистики

### Ошибки
- Проверять Vercel Logs на наличие ошибок
- Мониторить статус деплоев

---

## 🔄 ОБНОВЛЕНИЕ ДАННЫХ

### Еженедельное обновление CSV

1. **Обновить данные**
   - Обновить `public/data/nhs_waits.csv`
   - Обновить `public/data/private_costs.csv`
   - Обновить `public/data/clinics.csv`

2. **Commit и push**
   ```bash
   git add public/data/*.csv
   git commit -m "Update data: $(date +%Y-%m-%d)"
   git push
   ```

3. **Vercel автоматически пересоберет сайт**

---

## 🌐 КАСТОМНЫЙ ДОМЕН

### Добавить домен в Vercel

1. **В настройках проекта Vercel**
   - Перейти в "Settings" → "Domains"
   - Добавить домен (например, `comparethewait.co.uk`)

2. **Настроить DNS**
   - Добавить A record или CNAME record
   - Следовать инструкциям Vercel

3. **Обновить переменную окружения**
   ```
   NEXT_PUBLIC_BASE_URL=https://comparethewait.co.uk
   ```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

Перед объявлением сайта готовым:

- [ ] Сайт задеплоен на Vercel
- [ ] Все страницы работают
- [ ] Sitemap.xml доступен
- [ ] Robots.txt доступен
- [ ] Google Search Console настроен
- [ ] Sitemap загружен в Search Console
- [ ] Кастомный домен настроен (если нужен)
- [ ] Google Analytics настроен (опционально)
- [ ] Lighthouse audit пройден
- [ ] Все ссылки работают
- [ ] Mobile responsive проверен
- [ ] Accessibility проверена

---

## 🆘 TROUBLESHOOTING

### Проблема: Build fails
**Решение:**
- Проверить ошибки в Vercel logs
- Убедиться что все зависимости установлены
- Проверить что CSV файлы в правильной папке

### Проблема: 404 на comparison pages
**Решение:**
- Проверить что `generateStaticParams()` работает
- Убедиться что все комбинации в данных
- Проверить что slug'ы правильные

### Проблема: Метаданные не работают
**Решение:**
- Проверить что `generateMetadata()` экспортирован
- Убедиться что переменные окружения настроены
- Проверить что данные загружаются корректно

---

**Готово!** 🎉

Сайт готов к запуску. После деплоя следуйте инструкциям выше для настройки мониторинга и SEO.

