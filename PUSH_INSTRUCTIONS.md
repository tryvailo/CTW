# 🚀 ИНСТРУКЦИИ ПО ЗАГРУЗКЕ В РЕПОЗИТОРИЙ

**Репозиторий:** https://github.com/tryvailo/CTW

## ✅ ЧТО УЖЕ СДЕЛАНО

- ✅ Git репозиторий инициализирован
- ✅ Все файлы закоммичены (2 коммита)
- ✅ Remote добавлен: `git@github.com:tryvailo/CTW.git`
- ✅ Ветка: `main`

## 🔐 ВЫПОЛНИТЕ PUSH ВРУЧНУЮ

Требуется аутентификация GitHub. Выполните одну из команд ниже:

### Вариант 1: HTTPS (с Personal Access Token)

```bash
# Переключитесь на HTTPS
git remote set-url origin https://github.com/tryvailo/CTW.git

# Push (GitHub запросит username и token)
git push -u origin main
```

**Если запросит пароль:**
- Username: ваш GitHub username
- Password: используйте Personal Access Token (не пароль!)
- Создайте token: https://github.com/settings/tokens

### Вариант 2: SSH (если настроен SSH ключ)

```bash
# Убедитесь что remote на SSH
git remote set-url origin git@github.com:tryvailo/CTW.git

# Push
git push -u origin main
```

**Если SSH не настроен:**
- Создайте SSH ключ: `ssh-keygen -t ed25519 -C "your_email@example.com"`
- Добавьте в GitHub: https://github.com/settings/keys

### Вариант 3: GitHub CLI (если установлен)

```bash
gh auth login
git push -u origin main
```

## 📋 БЫСТРАЯ КОМАНДА

Если у вас уже настроена аутентификация, просто выполните:

```bash
git push -u origin main
```

## ✅ ПРОВЕРКА ПОСЛЕ PUSH

После успешного push:

1. Проверьте репозиторий: https://github.com/tryvailo/CTW
2. Убедитесь что все файлы загружены
3. Подключите к Vercel для деплоя

---

**Готово! Выполните push командой выше.** 🚀

